{% extends 'base.html.twig' %}

{% block title %}Hello CityController!{% endblock %}

{% block body %}
    <div class="container">
        <div class="col-md-6">
			<a href="{{ path('update_city', {id: city.id})}}">Modifier</a>
            <a href="{{ path('delete_city', {id: city.id})}}">Supprimer</a>
            <h1>Welcome to {{ city.name }} ! ✅</h1>
            <p>{{ city.description }}</p>
            <p>{{ city.name }} fait partie du pays : {{ city.country.getName() }} <img src="{{ city.country.flag|pathFileFlags }}"></p>
            <a href="{{ city.link }}">Site officiel de la ville</a>					
		</div>
    </div>
{% endblock %}
