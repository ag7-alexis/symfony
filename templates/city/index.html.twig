{% extends 'base.html.twig' %}

{% block title %}Hello CityController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>Liste des villes ! ✅</h1><a href="{{ path('new_city') }}">test</a>
        </a></button>

        {% if cities is empty %}
            <p>Aucune ville</p>
        {% else %}
            <ul>
                {% for city in cities %}
                    <li><a href="{{ path('city', {cityName: city.name}) }}">{{ city.name | upper }}</a></li>
                {% endfor %}
            </ul>
        {% endif %}

    </div>
{% endblock %}
